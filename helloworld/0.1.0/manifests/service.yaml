apiVersion: v1
kind: Service
metadata:
  name: helloworld
  namespace: "{{ .Values.pack.namespace }}"
  labels:
    app: helloworld
spec:
  type: ClusterIP
  selector:
    app: helloworld
  ports:
    - name: http
      port: 80
      targetPort: http
